# The default, in-process MVP verifier.
VERIFIER_PROVIDER=mathjs

# Future Python service configuration. Browser clients keep calling /api/verify.
# VERIFIER_PROVIDER=sympy
# SYMPY_VERIFIER_URL=http://127.0.0.1:8000/verify

# Keep this server-side only. The current teaching fallback works without it.
# OPENAI_API_KEY=
